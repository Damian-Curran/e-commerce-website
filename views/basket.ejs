<ul>
    <li ng-repeat="item in itemBasket">
          <div>
                  {{item.name}}
                  <img src="/uploads/{{item.image1}}" width="200" height="200">
          </div>
    </li>
  </ul>